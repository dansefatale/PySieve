Pysieve is simple wrapper of the msieve prime factorization library.
It basically gives you a fast factor command and some utilities. For
now it only supports python 2.6 and upwards and compiles on OSX with 
msieve installed via macports. Very basic.
